<Window x:Class="LibraryManagement.UI.BookView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LibraryManagement.UI"
        mc:Ignorable="d"
        Title="BookView" Height="450" Width="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical">
            <TextBlock Text="Title"/>
            <TextBox x:Name="txtTitle"/>

            <TextBlock Text="Author" Margin="0,5,0,0"/>
            <TextBox x:Name="txtAuthor"/>

            <TextBlock Text="Category" Margin="0,5,0,0"/>
            <TextBox x:Name="txtCategory"/>

            <TextBlock Text="Quantity" Margin="0,5,0,0"/>
            <TextBox x:Name="txtQuantity"/>

            <TextBlock Text="Search" Margin="0,5,0,0"/>
            <TextBox x:Name="txtSearch"/>

            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                <Button Content="Add" Click="btnAdd_Click" Width="80"/>
                <Button Content="Update" Click="btnUpdate_Click" Width="80" Margin="5,0"/>
                <Button Content="Delete" Click="btnDelete_Click" Width="80"/>
                <Button Content="Search" Click="btnSearch_Click" Width="80" Margin="5,0"/>
            </StackPanel>
        </StackPanel>

        <DataGrid x:Name="dgBooks"
              Grid.Row="1"
              AutoGenerateColumns="True"
              Margin="0,10,0,0"
              SelectionChanged="dgBooks_SelectionChanged"/>
    </Grid>
</Window>
