<UserControl x:Class="LibraryManagement.UI.DashboardAdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LibraryManagement.UI"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800" Background="White">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="HỆ THỐNG THỐNG KÊ THƯ VIỆN" 
                   FontSize="24" FontWeight="Bold" Foreground="#2C3E50"
                   Margin="0,0,0,20" HorizontalAlignment="Center"/>

        <UniformGrid Grid.Row="1" Columns="2" Rows="2" Margin="0,0,0,20">

            <Border Background="#3498DB" CornerRadius="10" Margin="10" Padding="15">
                <StackPanel>
                    <TextBlock Text="TỔNG SỐ SÁCH" Foreground="White" FontWeight="SemiBold"/>
                    <TextBlock x:Name="txtTongSoSach" Text="0" FontSize="35" Foreground="White" FontWeight="Bold" HorizontalAlignment="Right"/>
                </StackPanel>
            </Border>

            <Border Background="#2ECC71" CornerRadius="10" Margin="10" Padding="15">
                <StackPanel>
                    <TextBlock Text="TỔNG NGƯỜI MƯỢN" Foreground="White" FontWeight="SemiBold"/>
                    <TextBlock x:Name="txtTongNguoiMuon" Text="0" FontSize="35" Foreground="White" FontWeight="Bold" HorizontalAlignment="Right"/>
                </StackPanel>
            </Border>

            <Border Background="#F1C40F" CornerRadius="10" Margin="10" Padding="15">
                <StackPanel>
                    <TextBlock Text="SÁCH MƯỢN NHIỀU NHẤT" Foreground="White" FontWeight="SemiBold"/>
                    <TextBlock x:Name="txtSachMuonNhieu" Text="..." FontSize="18" Foreground="White" FontWeight="Bold" Margin="0,10,0,0" TextWrapping="Wrap"/>
                </StackPanel>
            </Border>

            <Border Background="#E67E22" CornerRadius="10" Margin="10" Padding="15">
                <StackPanel>
                    <TextBlock Text="NGƯỜI MƯỢN NHIỀU NHẤT" Foreground="White" FontWeight="SemiBold"/>
                    <TextBlock x:Name="txtNguoiMuonNhieu" Text="..." FontSize="18" Foreground="White" FontWeight="Bold" Margin="0,10,0,0" TextWrapping="Wrap"/>
                </StackPanel>
            </Border>

        </UniformGrid>

        <GroupBox Grid.Row="2" Header="DANH SÁCH SÁCH QUÁ HẠN" FontWeight="Bold" Foreground="#E74C3C">
            <DataGrid x:Name="dgSachQuaHan" Margin="10" AutoGenerateColumns="False" 
                      CanUserAddRows="False" Background="White" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Mã Sách" Binding="{Binding MaSach}" Width="100"/>
                    <DataGridTextColumn Header="Tên Sách" Binding="{Binding TenSach}" Width="*"/>
                    <DataGridTextColumn Header="Người Mượn" Binding="{Binding TenDocGia}" Width="*"/>
                    <DataGridTextColumn Header="Ngày Hết Hạn" Binding="{Binding NgayHenTra, StringFormat=dd/MM/yyyy}" Width="120"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

    </Grid>
</UserControl>